# Server port (mapped in docker-compose.yml)
server.port=9098

# Redis connection (service name from docker-compose.yml)
spring.redis.host=redis
spring.redis.port=6379

# Optional: logging level
logging.level.org.springframework=INFO
